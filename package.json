{
  "name": "node-express-web-ts",
  "version": "1.0.0",
  "description": "Jiocoders portfolio using node.js",
  "main": "index.ts",
  "scripts": {
    "dev": "nodemon --watch --ext 'ts,json' --exec 'npm run build && npm run start'",
    "copy-assets": "cp -r views dist && cp -r public dist",
    "build": "tsc && npm run copy-assets",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.3.2",
    "@types/node": "^20.10.0",
    "nodemon": "^3.0.1"
  }
}
